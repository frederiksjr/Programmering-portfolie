text = "lix skal analysre dette kode"
def LIX(txt):
    word = txt.split("")
    wordcount = len(word)
    highcharr = 0
    bigarray = []
    for i in range(wordcount):
        temparray = word[i]
        chararr = []
        for letter in temparr:
            chararr.append(letter)
        charcount = len(chararr)
        if charcount >= 6:
            bigarray.append(charcount)
            
